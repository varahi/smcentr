<div class="col-md-4">
    <div class="job_item">
        <a id="job{{ order.id }}"></a>
            {% include 'partials/order/_order_main_info.html.twig' %}
        <div class="client_contact_visible">
            {% include 'partials/order/_contact_info.html.twig' %}
        </div>
        {% if order.users %}
        <div class="client_address_visible">
            {{ order.address }}
        </div>
        {% endif %}
    </div>
</div>